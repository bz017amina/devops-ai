apiVersion: v1
kind: Service
metadata:
  name: credit-api-service
spec:
  selector:
    app: credit-risk
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    nodePort: 32542